import { topicAdvscriptPackagesVitePlugin } from "./vite-plugin";

describe("topicAdvscriptPackagesVitePlugin", () => {
  it("should work", () => {
    expect(topicAdvscriptPackagesVitePlugin()).toEqual("topic-advscript-packages-vite-plugin");
  });
});
