// import { getCurrentInstance } from "vue-demi";

// export function useExtrenalInstance<T>(getter: T, ins = getCurrentInstance()) {
//   const proto = Object.getOwnPropertyDescriptors(getter);
//   Object.defineProperties(ins.proxy, proto);
//   return ins.proxy as T & Vue;
// }
